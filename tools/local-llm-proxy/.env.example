# 本机 LLM 代理端口
PORT=8787

# 目标 LLM 服务（OpenAI-compatible）
PROVIDER_BASE_URL=https://api.openai.com/v1

# 目标 LLM API Key（只保存在本机，不上传 wrong-notebook）
PROVIDER_API_KEY=sk-your-key-here

# 允许跨域访问的页面地址
# 本地开发用 http://localhost:3000
# 如果 web 页面部署在其他地址，改为对应地址
ALLOWED_ORIGIN=http://localhost:3000
